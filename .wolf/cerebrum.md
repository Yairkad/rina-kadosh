# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-07-24

## User Preferences

- **המשתמש הוא גבר** — לא להשתמש בלשון נקבה בפנייה אליו.
- **Vercel workflow:** אחרי כל `git push` — לבדוק סטטוס deployment ב-Vercel API. אם ERROR — לשלוף לוגים, לזהות errors, לתקן ולעשות push מחדש. הכל אוטומטי ללא שאלות למשתמש.
- **Supabase workflow:** (א) כשיש migration חדש — להריץ `mcp__supabase__apply_migration` לבד ללא אישור. (ב) אחרי כל migration — להריץ `mcp__supabase__get_advisors` ולבדוק אזהרות. אם יש — לדווח ולתקן. הכל אוטומטי.
- **סגנון תקשורת:** תמציתי וממוקד — לא לפרט יותר מדי, ישירות לעניין.
- **גישה לפיתוח:** לחשוב מראש על כל הקומפוננטות לפני תחילת קוד — לאסוף השראות תחילה.
- **סינון טכני:** כשמציגים רשימת שיפורים ארוכה — לסנן ולהסביר בקצרה מה כן/לא ומדוע.
- **עיצוב:** luxury + elegance + "wow ממבט ראשון". פלטה חמימה (לא קרה/סגולה). אנימציות מרשימות.
- **רספונסיביות:** חובה בכל דף — mobile-first עם עקביות ויזואלית ו-touch-friendly.
- **[2026-07-23] רקעים/טקסטורות מהדמיית Illustrator:** כשאפשר לשחזר בדיוק בקוד (צבע שטוח/גרדיאנט/דפוס גאומטרי פשוט) — המשתמש מעדיף קוד (CSS/SVG) על פני תמונה. תמונה רק כשיש טקסטורה אמיתית שלא ניתנת לשחזור.
- **[2026-07-24] צבעי מותג מדויקים (מהמשתמש, מהסווטצ'ים באילוסטרייטור):** טרהקוטה `#C85741` (`--terracotta`, hover `--terracotta-dark: #A8432E`), זית/סאג' `#B7B384` (`--olive`). מוגדרים ב-`app/globals.css` + `tailwind.config.ts` (`theme.extend.colors.terracotta/olive`).
- **[2026-07-27] "ירוק" בפי המשתמש = הזית/סאג' הקיים (`--olive` #B7B384), לא צבע חדש** — כשהמשתמש ביקש "הפס ניווט בצבע הירוק שהבאתי לך" לא היה נכס חדש בשום מקום (נבדק ATAR/ במלואו) — הפרשנות שיושמה: זית. אם יתברר שהתכוון לגוון אחר, לעדכן כאן.
- **[2026-08-26] פונט זוגי (font pairing) — Bona Nova SC לכותרות סקשן בלבד, Rubik לכל השאר.** המשתמש ביקש לנסות Bona Nova SC (Google Fonts, subset עברי) כפונט העברי הראשי, ראה שזה לא מתאים לניווט/שמות מוצרים/כפתורים, וביקש שילוב: `--font-heading-he` (Bona Nova SC, self-hosted כי next/font/google לא כולל את ה-variant SC) דרך מחלקת `font-heading` ב-Tailwind — כרגע על HeroSection h1, page.tsx events h2, ProcessSteps h2, FloatingProducts lead. `--font-hebrew` (Rubik) נשאר ברירת המחדל לכל שאר האתר (body-wide דרך `[dir="rtl"] body` ב-globals.css). **החלטה פתוחה:** לא כל כותרת באתר קיבלה `font-heading` עדיין — רק 4 המקומות בדף הבית שטופלו בסבב הזה; דפי קטלוג/מוצר/אדמין עדיין ב-Rubik לכל הטקסט.
- **[2026-07-24] לוגו רינה קדוש — PNG בלבד, סופי:** `public/images/logo.png` (מקור: `ATAR/logo.png`, 6183×4453, גוון זית, רקע שקוף). **אין SVG/וקטור ולא יהיה** — המשתמש אישר מפורשות. אל תציע/תתכנן שוב סבב "המתנה ל-SVG".

## Key Learnings

- **Project:** rina-kadosh
- **מבנה קטלוג:** 3 רמות — `/catalog` → `/catalog/[event]` → `/catalog/[event]/[style]` → `/catalog/[event]/[style]/[product]`. חבילות הן **סקשן** בתוך דף ה-style, לא דף נפרד.
- **אין product_variants:** כל מוצר קיים רק בסוג אחד מוגדר. אין גדלים/חומרים/וריאנטים.
- **מינימום הזמנה:** כל מוצר מגדיר `min_type: 'units' | 'amount'` + `min_value` — ולידציה בזמן אמת.
- **חבילה בסל:** ישות אחת (`is_bundle: true`), לא מתפרקת — כדי לשמור על תמחור החבילה.
- **Cart Drawer:** צד בדסקטופ, bottom sheet במובייל. כפתור צף + עמוד /cart נפרד — שניהם.
- **Quick-add:** desktop = hover overlay עם counter; mobile = כפתור `+` → mini modal.
- **סטטוס מוצר:** `draft | published | archived` — לא boolean.
- **Admin auth:** `profiles.is_admin` boolean — לא full RBAC.
- **תמונות:** `images[0]` = thumbnail. WebP/AVIF, srcset, blur placeholder.
- **מספור הזמנות:** פורמט `RK-YYYYMMDD-XXX`.
- **[2026-07-27] כוכב האתר = גליף "✦" בטרהקוטה, לא SVG/תמונה** — כבר בשימוש עקבי ב-InspirationSection (לפני מחיקה), בכרטיסי סגנון ב-`/catalog/[event]` (עיגון פינתי) ובתגית "חבילות" ב-`/catalog/[event]/[style]`, ותואם בדיוק את ה-✦ שמופיע בהדמיות Illustrator (`ATAR/‏‏לכידה*.JPG`). נעשה בו שימוש חוזר כמפריד בין פריטי הניווט ב-Navbar. אם המשתמש שולח קובץ כוכב ייעודי (SVG/PNG) בעתיד — להחליף את הגליף בכל המקומות הללו יחד, לא רק בניווט.
- **[2026-07-27] products.display_order (מיגרציה 014) — סדר מוצרים בתוך סגנון ניתן לגרירה** — נוסף עמודה + backfill לפי `created_at DESC` בכל `design_style_id`. גרירה (native HTML5 DnD, לא dnd-kit — נמנע מתלות חדשה) מיושמת ב-`CatalogManager.tsx` בתוך הרשימה המורחבת של סגנון; `reorderProducts()` ב-`app/admin/actions/products.ts` כותב `display_order` לפי אינדקס. דף הציבור (`/catalog/[event]/[style]`) עודכן לעשות `.order("display_order")` במקום `created_at`.
- **[2026-08-19] event_types.atmosphere_image חובר — היה עוד שדה "רדום" כמו og_image (bug-091 pattern)** — העמודה נוספה ב-migration 002 (2026-07-כלשהו) אבל מעולם לא הייתה בטופס העריכה של סוג אירוע ב-`CatalogManager.tsx` ולא נקראה ב-`/catalog/[event]/page.tsx` (שהשתמש תמיד ב-`/textures/marble-bg.jpg` קבוע). המשתמש ביקש רקע נבחר-לפי-קטגוריה — נוסף `showAtmosphereImage` לטופס "עריכת סוג אירוע", והדף הציבורי עכשיו בודק `eventType.atmosphere_image` (תומך גם וידאו לפי סיומת, כמו ב-design_styles) ונופל חזרה לשיש כשריק. **לקח כללי (שוב): כל עמודת DB חדשה שנוספת "כדי שיהיה מוכן להמשך" — לבדוק תמיד גם את טופס האדמין וגם את דף הציבור לפני שמניחים שהיא "כבר עובדת", כי היא נוספת פעמיים בפרויקט הזה בלי חיווט (og_image, ועכשיו atmosphere_image ברמת event_type).**
- **[2026-07-27] design_styles.og_image לא היה ניתן להעלאה מהאדמין — פער אמיתי, לא רק חוסר תיעוד** — העמודה נקראת בדף הציבורי (`/catalog/[event]/page.tsx`, עיגול התמונה על ריבוע הזית) כבר מההתחלה, אבל טופס עריכת סגנון ב-`CatalogManager.tsx` מעולם לא כלל שדה העלאה עבורה (רק `atmosphere_image`). נוסף `showOgImage` + חיווט ל-`createDesignStyle`/`updateDesignStyle`. **לבדוק תמיד שכל שדה שנקרא בדף הציבורי אכן ניתן לעריכה מהאדמין — אי-התאמה כזו שקטה ולא זורקת שגיאה, פשוט מציגה ריבוע בלי עיגול תמונה.**
- **[2026-08-19] כל אינדיקטור טעינה (loading spinner) בצד הציבורי — להשתמש בגליף ✦ (מסתובב), לא בטבעת CSS גנרית** — תוקן ב-`ExpandingEventCards.tsx` (היה `border-2 border-terracotta border-t-transparent animate-spin`, הפך ל-`<span>✦</span>` עם `animate-spin` + `animationDuration: "1.6s"` מותאם ידנית כי glyph לא-סימטרי מסתובב "רועש" יותר מטבעת בברירת המחדל 1s). תואם ל-[[design_references]]/cerebrum "כוכב האתר = ✦ בטרהקוטה, לא SVG". **הכלל: ספינרים אחרים בקוד (יש כ-10 קבצים עם animate-spin, כולם ב-components/admin/**) הם ספינרים אדמיניסטרטיביים על כפתורי שמירה — לא לגעת בהם בלי בקשה מפורשת, הבקשה הזו הייתה ספציפית לספינר הציבורי.**
- **[2026-08-19] `design_styles.atmosphere_image` תומך גם בוידאו עכשיו, לא רק תמונה/GIF** — עמודת ה-DB היא `text` (URL) יחידה שמכילה גם תמונה וגם וידאו; ההבחנה בצד הלקוח היא לפי סיומת הקובץ (`/\.(mp4|webm|mov)$/i`), לא עמודה/type נפרד. שונה: `ImageUpload.tsx` קיבל props אופציונליים `allowVideo`/`maxMB` (ברירת מחדל נשארת image-only/10MB לכל שאר השימושים ב-products/gallery — **לא** לשנות את ברירת המחדל), `/catalog/[event]/[style]/page.tsx` בודק סיומת ומרנדר `<video autoPlay muted loop playsInline>` במקום `<Image fill>`. `CatalogManager.tsx` מעביר `allowVideo maxMB={50}` רק לשדה "תמונת אווירה" (design_styles), לא ל-og_image. **אזהרה שנמסרה למשתמש, לא נבדקה בפועל:** מגבלת Supabase Storage free-tier לקובץ בודד (בד"כ 50MB) לא אומתה מול ה-dashboard בפועל (אין גישת Supabase MCP מאומתת בסשן זה) — אם ההעלאה נכשלת על קובץ גדול, לבדוק קודם את מגבלת ה-bucket ב-Supabase Dashboard לפני שמניחים שזה קוד.
- **Design reference — ExpandingCards:** `design-references/ExpandingCards.tsx` — כרטיסי grid עם הרחבה ב-hover. טרם הוחלט באיזה רמת קטלוג ישמש.
- **Design reference — CinematicFooter:** `design-references/CinematicFooter.tsx` — קוד שהמשתמש הדביק (לא נכתב עבור הפרויקט הזה; GSAP+ScrollTrigger, טוקני shadcn גנריים, תוכן placeholder של אפליקציה לא קשורה). המשתמש בחר לשלב רק חלק מהאפקטים — **שילוב כללי באתר, לא ספציפית לפוטר** (מיקום סופי טרם הוחלט): (1) כפתורים מגנטיים + סגנון glass-pill, (2) מרקיע גלילה עם משפטי מיתוג, (3) אפקט "וילון" בגלילה + כפתור חזרה למעלה. לפני שילוב בפועל: למפות טוקנים (`--primary`/`--secondary`/`--background`/`--foreground`/`--destructive`/`--border`/`--muted-foreground` → `--terracotta`/`--olive`/`--marble`/`--charcoal`/`--gold`), ולשקול מימוש עם framer-motion הקיים במקום להוסיף gsap כתלות חדשה. פירוט מלא ב-TODO plan: `C:\Users\יאיר קדוש\.claude\plans\memoized-wandering-sunrise.md` סעיף 5.
- **[2026-08-25, גרסה סופית] FloatingProducts — סקשן חדש בדף הבית, מיד אחרי HeroSection, נבנה לפי הדמיה מדויקת.** `components/home/FloatingProducts.tsx`. המשתמש שלח הדמיה (קנבס 2169×1000) + 5 נכסים חדשים דרך network share: 4 תמונות מוצר (`scroll-seal`/`gift-box`/`wax-seal`/`bencher-stand`) + `pearls-overlay.png` שהוא **בדיוק 2169×1000** — overlay מוכן-מראש של פנינים מפוזרות, בדיוק אותו יחס כמו הקנבס. **טכניקה:** הסקשן כולו `aspect-[2169/1000]`, וכל מיקום (תמונות/טקסט/פנינים) הוא % מתוך זה — כך הכל נשאר מיושר יחד בכל רוחב מסך בלי חישובים נפרדים לכל breakpoint. טקסט **מיושר לימין** (לא ממורכז) עם 3 פסקאות אמיתיות מהמשתמש. `float` keyframe ב-`tailwind.config.ts` `theme.extend` (לא `globals.css` — Tailwind v3).
  - **[באג + תיקון] `transform: translate(-50%,-50%)` inline + מחלקת `animate-float` על אותו אלמנט = קונפליקט.** CSS animation שמטרגטת `transform` דורסת את **כל** הערך, כולל סנטור inline שהוגדר בנפרד — האלמנט "קופץ" למיקום top-left+offset קטן במקום להישאר ממורכז ואז לרחף. **הפתרון: 2 divs מקוננים** — חיצוני עושה מיקום (`left`/`top` % + `-translate-x-1/2 -translate-y-1/2` כ-Tailwind **class**, לא inline), פנימי מקבל רק `animate-float`. כלל כללי: אף פעם לא inline `transform` + Tailwind animation class על אותו אלמנט — לפצל לשני elements.
  - **[2026-08-26, האבחון האמיתי נמצא] "קריעה" ב-gift-box/wax-seal — לא היה קשור בכלל ל-CSS/אנימציה. הבעיה האמיתית: קבצי המקור עצמם.** בזבזתי 3 סבבי תיקון (מיקום drop-shadow פעמיים, הסרת scale מהפנינים) על תיאוריות CSS לפני שהאבחון הנכון עלה. **מה שבאמת קרה:** `gift-box.png` ו-`wax-seal.png` (Artboard 13/14 מקובץ Illustrator) יוצאו עם crop לא מדויק — כל אחד "דימם" פיסה קטנה מה-artboard השכן שלו לתוך הפינה שלו (houndstooth של wax-seal בפינה הימנית-תחתונה של gift-box; ריבוע ירוק/ורוד של gift-box בפינה השמאלית-עליונה של wax-seal). זו לא הייתה תקלת רינדור בכלל — זה נראה בדיוק כמו "משהו נחתך ונוסף" כי זה היה **פשוטו כמשמעו** קטע שלא שייך לתמונה. **איך זיהיתי לבסוף:** המשתמש אמר "זה רק ב-2 האלמנטים מימין" (gift-box+wax-seal, לא scroll-seal/bencher) — זה בדיוק הרמז: שני הפריטים שהיו artboards **שכנים** במקור. וגם — כבר **ראיתי** את הפרגמנט הזה ב-gift-box.png בסבב אבחון קודם ולא פעלתי לפיו, פשוט המשכתי לתיאוריית CSS. **לקח קריטי: כשמתאר משתמש "חיתוך/הוספת חלקים" בתמונה — לבדוק את קובץ ה-PNG המקורי (Read ישיר על הקובץ) *לפני* כל תיאוריית CSS/רינדור/אנימציה, במיוחד אם התופעה עקבית (לא משתנה בין רענונים) ומוגבלת לפריטים ספציפיים ולא לכל האלמנטים המונפשים. תיקון: `PowerShell`/`System.Drawing` עם `Graphics.CompositingMode=SourceCopy` + `FillRectangle(Color.Transparent, ...)` על אזור מלבני קטן בפינה הרלוונטית — לא crop מלא (כי זה משנה יחס-רוחב/מיקום העוגן), רק "מחיקה" נקודתית לשקיפות.**
  - **[2026-08-26, תוקן שוב 2026-08-26] `drop-shadow` (filter) על ANCESTOR של אלמנט מונפש = עדיין קורע, גם כשה-filter לא על אותו אלמנט כמו ה-animation.** ניסיון ראשון: העברתי את `drop-shadow-xl` ל-wrapper החיצוני הסטטי (שעוטף את ה-div עם `animate-float`) — **לא פתר את זה, המשתמש דיווח שהבאג עדיין קיים.** הסיבה: `filter` מחשב מחדש לפי ה-pixels המצוירים בפועל של **כל תת-העץ שלו**, אז גם אם ה-filter יושב על אבא סטטי, ברגע שהילד הפנימי זז ב-transform מונפש, כרום עדיין צריך לחשב מחדש את ה-filter (כולל alpha-tracing של הצל) **בכל frame** — בדיוק אותה תקלה, רק מוסתרת ברמת element אחרת. **הפתרון הנכון בפועל: ה-filter חייב לשבת על LEAF שאין לו צאצאים מונפשים משלו** — כאן: על ה-`<Image>` עצמו, בתוך ה-div עם `animate-float` (לא מעליו, לא לידו). כך ה-filter מחושב פעם אחת (התוכן של ה-Image סטטי), וה-transform האב רק "סוחב" את הביטמאפ המסונן המוכן. **כלל מדויק יותר מהניסיון הקודם: filter צריך לשבת הכי עמוק שאפשר בעץ, אחרי (בפנים מ-) כל transform מונפש — לא "בנפרד מ", אלא "בתוך" הקונטיינר המונפש, על אלמנט leaf.** אימות: לא ניתן לוודא עם screenshot סטטי (זו תקלה שקורית רק תוך כדי frame-ים מסוימים של האנימציה) — צריך אישור חי מהמשתמש בדפדפן.
  - **[2026-08-26] פונט Hebrew multi-script "SC" (small caps) — ההבדל הוויזואלי לעברית עשוי להיות עדין, לא דרמטי כמו בלטינית.** Small caps הוא feature לטיני בלבד (עברית אין לה אותיות גדולות/קטנות) — כשמחליפים פונט Hebrew ל-variant "SC" של Google Font, ה-glyphs העבריים בפועל עשויים להיראות קרובים לגרסה הרגילה. לפני שמסיקים "זה לא עובד" מסקרינשוט — לוודא תחילה שכל שרשרת הטעינה תקינה (woff2 נטען 200, @font-face מוגדר, CSS var מוקצה, utility class בקוד המקומפל, class על ה-DOM element) לפני שמניחים שהבעיה בקוד; אם הכל תקין, ההבדל פשוט עדין יותר משציפו — לא להניח כשל בלי בדיקה טכנית מלאה.
  - **[2026-08-26] עטיפת סקשן קיים ב-wrapper div חדש (למשל לרקע משותף) יכולה לשבור `-z-index` שלילי בתוכו.** כשעטפתי את סקשן ה-event-cards ב-`<div className="bg-marble...">` חדש (למטה), ה-`GeometricPattern` בפנים (`-z-10`) נעלם — כי z-index שלילי דורש stacking context כדי "לדעת" ביחס למה הוא שלילי, וה-wrapper החדש לא יצר כזה (סתם `relative` בלי z-index לא יוצר stacking context). **פתרון: `isolate` (Tailwind, = `isolation:isolate`) על ה-`<section>` עצמו** — כופה stacking context מקומי כך שה-`-z-10` בפנים חוזר להתייחס נכון לתוכן של אותו סקשן. **כלל: בכל פעם שעוטפים סקשן קיים שיש בו z-index שלילי ב-div הורה חדש — לבדוק/להוסיף `isolate` על הסקשן, לא להניח שזה עדיין יעבוד.**
  - **רקע משותף בין סקשנים סמוכים.** כשהמשתמש מבקש "יחידה אחת בלי מעבר/הפרדה" בין 2 סקשנים סמוכים — **לא** לתת לכל אחד `bg-marble` נפרד (כל `bg-cover` נגזר ביחס לגובה/רוחב האלמנט שלו, אז 2 אלמנטים נפרדים = 2 crops שונים של אותה טקסטורה = תפר). הפתרון: לעטוף את שניהם ב-`<div>` הורה **אחד** עם הרקע, ולהסיר רקע מהילדים — רק כך זה אותה תמונת רקע רציפה בפועל.
- **Hero — PrismaHero:** נדון בשיחה, המשתמש אהב את ה-wow factor אבל הפלטה הקרה/סגולה לא מתאימה — לא אומץ.
- **[2026-08-25, מעודכן שוב 2026-08-25] Hero — קרוסלת התמונות הוסרה זמנית, לא "סגור" יותר.** אחרי שהמשתמש אמר במפורש "התמונות לא איכותיות ולא יפות, תסיר עד שאביא אחרות" — `HeroSection.tsx` שונה בחזרה מקרוסלת 3 תמונות (crossfade) לרקע `bg-marble` (טקסטורת השיש הקיימת, `bg-cover bg-center`, אותה class בדיוק כמו `FloatingProducts.tsx` שמתחתיו — כדי שהשניים ייראו כמשטח רציף אחד בלי הפרדה, לפי בקשה נפרדת של המשתמש). תוכן ה-Hero (מונוגרם+כותרת+כפתורים) הוגדל (`w-28 sm:w-36`→`w-40 sm:w-52 md:w-60` למונוגרם, `text-2xl..text-4xl`→`text-3xl..text-5xl` לכותרת) ומורכז פיזית (`left:14%`→`left-1/2 -translate-x-1/2`, היה pinned לשמאל קודם). **ממתין:** תמונות hero חדשות איכותיות מהמשתמש — וגם תמונת mobile ייעודית (לא רק scale-down של desktop, המשתמש ביקש זאת במפורש). ראה [[todo-2026-08-25]] פריט חדש.

## Do-Not-Repeat

- **[2026-08-25] אלמנטים עם `initial={{opacity:0}}` + `animate`/`whileInView` לא ייראו ב-openwolf designqc screenshot — זו לא בהכרח בעיית קוד** — Framer Motion מיישם את ה-`initial` state כ-inline style גם ב-SSR (`style="opacity:0;transform:translateY(24px)"` מאומת ב-HTML הגולמי דרך curl), והאנימציה בפועל ל-`animate`/`whileInView` רק רצה אחרי hydration בצד לקוח. כלי ה-screenshot (headless) לפעמים לוכד את הדף **לפני** שה-hydration+אנימציה הספיקו לרוץ — נבדק וקרה עקבית 4 פעמים ברצף עם `HeroSection.tsx` (מונוגרם+כותרת עם `whileInView`), גם אחרי שהשרת הגיב מהר (0.7s). **לפני שמניחים שיש באג אמיתי:** לבדוק את ה-HTML הגולמי (`curl` + `grep` לטקסט/רכיב הרלוונטי) — אם הטקסט/התמונה קיימים ב-HTML (רק עם `opacity:0` inline), זה כנראה timing של הכלי, לא קוד שבור. **טריק אימות מהיר — תוקן [2026-08-25]:** ניסיתי קודם להחליף `whileInView`→`animate` בלבד (כמו שכתוב למעלה) ב-`FloatingProducts.tsx` — **זה לא עבד**, ה-screenshot עדיין תפס `opacity:0` כי גם עם `animate` (ללא scroll-trigger) ה-SSR עדיין מרנדר את ה-`initial` state ב-inline style, וה-hydration+transition (אפילו 0.6s) עדיין לא הספיקו לרוץ עד שה-headless browser צילם. **הפתרון שבאמת עבד: `initial={false}`** (לא `animate` במקום `whileInView`) — זה מדלג לגמרי על ה-initial state ב-SSR, אז הטקסט מופיע מיד ב-HTML הגולמי בלי `opacity:0`. תמיד להוסיף `initial={false}` לבדיקה, לא להחליף את ה-trigger prop (`whileInView`/`animate`) — ואז **להחזיר** ל-`initial={{opacity:0,...}}` המקורי לפני שמסיימים, בדיוק כמו קודם.
- **[2026-08-25] `openwolf designqc` מול שרת `next dev` שרץ הרבה זמן — flaky, לא דטרמיניסטי, בעיקר ב-mobile viewport.** באותה סשן, עם אותו קוד ואותו שרת, ~50% מהניסיונות נכשלו ב-"Navigation timeout of 30000 ms exceeded" ו-~50% הצליחו — נבדק שההצלחה/כישלון לא מתואם עם עריכת קובץ קודם (ניסיתי "לחמם" עם `curl --max-time 90` לפני כל ניסיון, לא פתר את זה באופן עקבי). **תבנית שנצפתה:** desktop viewport הצליח בעקביות רבה יותר מ-mobile viewport (באותה ריצה, לפעמים desktop הצליח ו-mobile נכשל). **מה שכן עוזר:** הפעלה מחדש מלאה של שרת ה-dev (kill + `npm run dev` חדש) פתרה כישלון מוחלט אחד שבו ה-CSS המקומפל בפועל חסר utility class חדש שנוסף ל-`tailwind.config.ts` (הCSS שהוגש היה stale). **אם designqc נכשל שוב ושוב:** לנסות שוב 2-3 פעמים ברצף בלי לשנות קוד באמצע — לרוב אחת מהן מצליחה; לא לבזבז יותר מדי ניסיונות על mobile ספציפית אם desktop כבר אומת, ולתעד verbal לבדיקה ידנית של המשתמש.
- **[2026-08-25] RTL: בלוק עם `position: absolute` בלי `left`/`right` מפורש נצמד אוטומטית לימין, לא לשמאל** — נתקלנו בזה ב-`HeroSection.tsx`: div עם `max-w-md` (בלי `mx-auto`/`float`/flex-positioning) בתוך דף עם `dir="rtl"` על ה-`<html>` נצמד לקצה ה-inline-start שבתרגום RTL הוא **ימין**, לא שמאל כמו שאינטואיציה מ-LTR הייתה מצפה. Tailwind logical classes (`ps-`, `start-`, `text-start` וכו') ממשיכים את אותה התנהגות. **הפתרון שעבד:** מיקום עם ערך **פיזי** מפורש — `style={{ left: "14%" }}` (או Tailwind `left-[]`, לא `start-[]`) — זה עוקף את מיפוי ה-RTL הלוגי לגמרי ונותן שליטה מדויקת בצד הפיזי של המסך, רלוונטי לכל קומפוזיציה שצריכה למקם אלמנט במקום ספציפי-פיזי (למשל ליישור עם רכיב עיצובי ברקע) בלי קשר לכיוון הטקסט.
- **[2026-08-20] אסור לפרוס קוד שמוסיף עמודת DB חדשה לשאילתת SELECT בעמוד ציבורי לפני שהמיגרציה רצה בפועל** — אם ה-`.select("...")` כולל עמודה שלא קיימת עדיין ב-DB, השאילתה נכשלת לגמרי (`42703`) וזה שובר את כל העמוד בפרודקשן, לא רק "לא מציג את השדה החדש". סדר נכון: (1) לכתוב את קובץ המיגרציה, (2) לבקש מהמשתמש להריץ אותו ידנית ב-SQL Editor (אין גישת DB-write בסשן הזה — לא Supabase MCP מאומת ולא CLI מקושר), (3) **לאמת בפועל** מול ה-REST API שהעמודה קיימת (curl עם anon key מספיק, `column ... does not exist` = עדיין לא רץ), **ורק אז** לעשות commit+push. נעשה נכון פעם אחת (background_image, 2026-08-19) — לשמור על אותו סדר בכל פעם שמוסיפים עמודה.

- **[2026-08-19] נסיון proxy route (`/api/media`) לעקוף חסימת Netfree על וידאו מ-supabase.co — נכשל ונמחק, אל תבנה מחדש** — הרעיון: להעביר את קובץ הווידאו של `atmosphere_image` דרך API route באתר עצמו (same-origin) במקום קישור ישיר ל-Supabase Storage, כי נטפרי (ומסנני תוכן ישראליים דומים) חוסמים לפי סיווג domain, ו-`*.supabase.co` הוא domain משותף שעלול להיות מסווג בעייתי. **בבדיקה מקומית:** ה-proxy עצמו נכשל עם 418 מ-Cloudflare (כנראה חסימת בוט לפי TLS fingerprint, לא הדרים) — נבדק גם ב-edge runtime וגם ב-node, שניהם נכשלו באותה צורה. **בפרודקשן (אחרי push):** המשתמש דיווח "ככה לא עובד בכלל" — כלומר הבעיה לא הייתה ספציפית לסביבת הפיתוח המקומית, ה-proxy לא עבד גם שם. **הפתרון בפועל שהמשתמש מצא: לא קוד בכלל** — פנה לנטפרי וביקש אישור/הסרת חסימה ידנית לקישור הספציפי מ-Supabase, וזה עבד. **מסקנה: אם עולה שוב בעיית Netfree/מסנן תוכן על נכס מדיה מ-Supabase Storage — הפתרון המהיר והאמין הוא לבקש מהמשתמש לפנות ישירות לנטפרי (יש להם מנגנון בקשת אישור אתר/קישור), לא לנסות שוב proxy route בצד השרת — זה נכשל פעם אחת כבר בשתי סביבות שונות (dev + prod) מאותה סיבה (חסימת בוט של Cloudflare על הבקשה השרת-לשרת, לא קשור להדרים שנשלחים).** קוד ה-proxy (`app/api/media/[...path]/route.ts`, `lib/utils.ts::toProxiedMediaUrl`) הוסר לגמרי מהקוד ב-commit שאחרי.
- **[2026-08-19] `catalog.ts` (event_types/design_styles CRUD) לא ריענן את העמודים הציבוריים — כל עריכה נשארה "תקועה" עד 300s** — `products.ts`/`bundles.ts`/`gallery.ts` כולם עושים `revalidatePath("/he/catalog", "layout")` + `revalidatePath("/en/catalog", "layout")` בנוסף ל-`/admin/...` אחרי כל mutation — אבל `catalog.ts` (createEventType/updateEventType/deleteEventType/createDesignStyle/updateDesignStyle/deleteDesignStyle/quickCreateProduct) עשה `revalidatePath("/admin/catalog")` בלבד, מעולם לא ריענן את הצד הציבורי. תוקן (bug-155) — נוספו שתי השורות הציבוריות לכל 7 הפונקציות. **מסקנה כללית: כל admin action חדש שכותב לטבלה שמוצגת גם בציבור (event_types/design_styles/products/bundles/gallery) חייב לכלול `revalidatePath` גם לעמודים הציבוריים הרלוונטיים (`/he/...`, `/en/...` עם `\"layout\"` כשיש נתיבים דינמיים מקוננים) — לא רק ל-`/admin/...` — לבדוק את זה מול products.ts כדוגמת ה-reference.**
- **[2026-08-19] אימות קובץ בהעלאה: לעולם לא לסמוך רק על `file.type` (MIME שהדפדפן מדווח)** — במיוחד עבור וידאו: `.mov` (סטנדרט iPhone) לפעמים מגיע עם `file.type` ריק/שגוי ב-Windows/דפדפנים מסוימים, מה שגורם לבדיקת `ALLOWED.includes(file.type)` להיכשל בשקט ולדחות קובץ תקין (המשתמש חושב שההעלאה "עברה" כי אין קריסה, רק הודעת שגיאה קטנה שקל להחמיץ). תוקן ב-`ImageUpload.tsx` (bug-155): הבדיקה עכשיו מקבלת גם lookup לפי סיומת קובץ (`EXT_MIME` map) כ-fallback, ומעבירה `contentType` מפורש ל-Supabase `.upload()` מבוסס הסיומת ולא ה-MIME הגולמי. **לכל שדה קלט קובץ עתידי (לא רק תמונות) — לוודא ולידציה כפולה (MIME + סיומת), לא MIME בלבד.**
- **[2026-08-19] `text-[var(--x)]/NN` (color-opacity modifier על צבע CSS-var שרירותי) לא תמיד מצייר בפועל — עדיף `text-[var(--x)] opacity-NN` נפרד** — ב-`ProcessSteps.tsx` נסיתי `text-[var(--gold)]/10` לוואטרמארק מספר דהוי; ב-screenshot (openwolf designqc, headless Chromium) זה רונדר כטקסט מלא-אטימות/כהה, לא דהוי כלל — כנראה `color-mix()` (המנגנון של Tailwind ל-opacity modifier על ערך שרירותי) לא נתמך/נכשל בשקט בסביבת הרינדור ההיא. **תוקן** ע"י פיצול ל-2 מחלקות נפרדות: `text-[var(--gold)] opacity-10` (CSS `opacity` רגיל, נתמך אוניברסלית, לא תלוי ב-color-mix). דפוסים קיימים כמו `text-[var(--charcoal)]/70` (בקוד המקורי) עבדו תקין באותה בדיקה — אז זו לא בעיה גורפת בכל שימוש, אבל **לכל שימוש חדש ב-opacity modifier על צבע arbitrary (במיוחד לאפקט עדין/דהוי כמו watermark), עדיף מראש להשתמש ב-`opacity-NN` נפרד ולא לסמוך על ה-slash modifier** — ולוודא ויזואלית (screenshot, לא רק tsc) לפני שסוגרים.

- **[2026-07-24, נפתר 2026-08-19] ✅ פונט mekomi (עברית, ללא רישיון web) הוחלף ב-Rubik ונמחק — happyBirthday (אנגלית) אושר כלא דורש רישיון, נשאר** — `app/fonts/mekomi/*.otf` הוטמע באתר (next/font/local) לצורך בדיקות בלבד, ללא רישיון web. ב-2026-08-19 המשתמש ביקש להחליף את פונט העברית בגלל חוסר הרישיון; נבחר **Rubik** (Google Fonts, רישיון פתוח, בלי צורך ברכישה) דרך `next/font/google`. שונה ב-4 קבצים: `app/fonts/index.ts` (export `rubik` במקום `mekomi`), `app/[locale]/layout.tsx` (import + `body` className), `tailwind.config.ts` (`heading` → `var(--font-hebrew)`), `app/globals.css` (`[dir="rtl"] body` → `var(--font-hebrew)`). שם משתנה ה-CSS שונה מ-`--font-mekomi` ל-`--font-hebrew` (סמנטי, כמו `--font-script` הקיים) כדי שהחלפת פונט עתידית לא תדרוש שוב rename. **קבצי `app/fonts/mekomi/*.otf` נמחקו לגמרי** (היו untracked ב-git, לא נכללו אף פעם בהיסטוריה) לפי בקשת המשתמש. `happyBirthday.ttf` (הפונט האנגלי/script) אושר במפורש על ידי המשתמש שאינו דורש רישיון — נשאר ללא שינוי. **הלקח הכללי נשאר בתוקף לכל פונט מותאם עתידי:** לפני כל push שכולל שינוי ב-app/fonts/, לוודא שהפונט שנטען בפועל (local file או Google Fonts) לא דורש רישיון web שלא נרכש.

- **[2026-08-08] Supabase ננעל למרות keep-alive cron — שני לקחים, לא רק אחד (ראה bug-136)** — (1) `app/api/keep-alive/route.ts` תוקן ב-23.7 ל-fail-closed (500 אם `CRON_SECRET` חסר, *לפני* פנייה בפועל ל-Supabase) — אבל **זה נשאר השערה לא מוכחת**: בדיקה חיה ב-Vercel dashboard (כפתור "Run" על אותו קוד ישן, בלי שום שינוי) החזירה 200 — כלומר לא הצלחנו להוכיח למפרע שה-secret אי-פעם באמת חסר. **הכלל שנשאר תקף בכל זאת:** endpoint שתכליתו למנוע כשל שקט (anon key, דאטה ציבורי, ללא מוטציה/PII) לא צריך fail-closed hard gate — תוקן ל-fail-open+warning ממילא, כי גם אם זו לא הייתה הסיבה בפועל הפעם, זה טרייד-אוף גרוע באופן עקרוני. (2) **הלקח שכן אושר בוודאות:** cron **שבועי** מול סף נעילה של Supabase של **7 ימים בדיוק** = אין שום מרווח ביטחון — Vercel Hobby בעצמו לא מתחייב לזמן ריצה מדויק ("flexible time window of 1-hour", מופיע בדשבורד Cron Jobs), אז ריצה בודדת שמתעכבת/נכשלת זמנית מספיקה לחצות את הסף ולנעול את הפרויקט **גם אם הקוד וה-secret תקינים לגמרי**. תוקן: תדירות שונתה ליומית (`0 3 * * *`) גם ב-`vercel.json` וגם בברירת המחדל של skill `supabase-keepalive` (SKILL.md + template) — כלל כללי לכל endpoint שנועד למנוע חציית סף זמן: לתזמן בתדירות גבוהה משמעותית מהסף, לא צמוד אליו.
- **[2026-05-27] אל תפנה למשתמש בלשון נקבה** — המשתמש הוא גבר. תמיד לשון זכר.
- **[2026-05-27] אל תוסיף טבלת product_variants** — המשתמש ביטל את הרעיון. כל מוצר קיים בסוג אחד מוגדר בלבד, אין גרסאות שונות.
- **[2026-05-27] חבילות אינן דף נפרד** — חבילות הן סקשן בתוך `/catalog/[event]/[style]`, לא `/bundles` עצמאי.
- **[2026-05-27] אל תציע Full RBAC / role system** — מנהל יחיד (רינה), is_admin boolean מספיק.
- **[2026-05-28] Supabase: RLS + GRANT שניהם נדרשים** — RLS policy לבד לא מספיק. חובה גם `GRANT SELECT ON table TO anon, authenticated` אחרת מקבלים `permission denied`. זה חל על כל טבלה חדשה שנוצרת.
- **[2026-05-28] ExpandingCards שייך לדף הבית** — המשתמש הבהיר שבחירת סוג אירוע עם הקומפוננטה צריכה להיות ישירות בדף הבית מתחת להירו, לא רק ב-/catalog.
- **[2026-05-28] Remove-Item עם סוגריים מרובעים** — PowerShell מפרש `[locale]` כ-wildcard. תמיד להשתמש ב-`-LiteralPath` כשהנתיב מכיל סוגריים מרובעים.
- **[2026-05-28] gallery_items סכמה שונה** — הטבלה כוללת `images` (ARRAY), `title_he`, `title_en`, `active` — לא `image_url`/`alt_he`/`alt_en`/`is_featured`.
- **[2026-05-28] אל תשתמש ב-Get-ChildItem בכלי Bash** — PowerShell cmdlets לא עובדים ב-Bash tool. להשתמש ב-`find` או בכלי Glob.
- **[2026-06-30] design_styles.slug אינו unique גלובלי** — הייחוד הוא `(event_type_id, slug)`. כל query על design_styles לפי slug בלבד חייב להוסיף `.eq("event_type_id", ...)` כדי למנוע PGRST116 כשיש מספר event_types עם אותו style slug.
- **[2026-06-30] חסר not-found.tsx** — ללא קובץ זה Next.js מנסה להציג 404 ברמת ה-document ומחוץ ל-locale layout, מה שגורם ל-HierarchyRequestError. חייב ליצור `app/[locale]/not-found.tsx`.
- **[2026-07-11] orders: WHEN clause בטריגר לא מטפל ב-NULL** — `trg_orders_order_number` רץ רק `WHEN (NEW.order_number = '')`. כש-order_number לא נשלח בכלל בעץ ה-insert הוא NULL, ו-`NULL = ''` ב-SQL הוא NULL (לא true) — אז הטריגר לא רץ ו-NOT NULL constraint נכשל. app/admin/actions/create-order.ts עבד כי הוא שולח `order_number: ""` במפורש; app/actions/submit-order.ts (העגלה הציבורית) לא שלח בכלל — נשבר תמיד, מוסתר עד עכשיו מאחורי bug-096 (GRANT). **מסקנה: כל INSERT חדש לטבלת orders (או טבלה עם טריגר דומה) חייב לשלוח את השדה במפורש כמו שdefault-ה-trigger מצפה, אל תניח ש-omit==empty-string ב-SQL.**
- **[2026-07-11] orders: RLS policy לבד לא הספיק גם כאן** — בדיוק כמו gallery_items (2026-05-28): migration 001 יצרה RLS policy "orders: anon insert" אבל בלי `GRANT INSERT ON orders TO anon` — ולכן שליחת הזמנה מהעגלה נכשלה עם "permission denied for table orders" (הוצג למשתמש כשגיאה כללית). תוקן ב-`007_orders_anon_insert_grant.sql`.
- **[2026-07-11] תיקון שגוי: "authenticated יש לו גישה כברירת מחדל"** — הנחתי (בטעות) ש-authenticated כבר יש לו table-level grants כברירת מחדל בפרויקט הזה, כי CRUD ניהולי ל-products/bundles/materials "עבד". התבדה מיד: יצירת קופון נכשלה עם `permission denied for table coupons` **גם עבור authenticated admin**. **מסקנה נכונה: אף role לא מקבל grants כברירת מחדל בפרויקט הזה על טבלאות שנוצרו ב-migration גולמי (בניגוד לטבלה שנוצרת דרך Supabase Studio table editor, שמקבלת grant אוטומטי). זה נתקל 3 פעמים נפרדות (gallery_items, orders, coupons).**
  - **ניסיתי לתקן עם GRANT גורף על כל הטבלאות + `ALTER DEFAULT PRIVILEGES` לכל טבלה עתידית — auto-mode חסם את זה בצדק** (`011_default_privileges_fix.sql` הוסר): זו הרחבת הרשאות schema-wide שהמשתמש לא ביקש, גם אם RLS עדיין הקו האמיתי. תוקן בחזרה ל-scope מינימלי: `GRANT ... ON coupons TO authenticated` בלבד (`011_coupons_authenticated_grant.sql`), בדיוק כמו gallery_items/orders.
  - **מסקנה לפעולה הבאה: כל טבלה חדשה שנוצרת ב-migration חייבת GRANT מפורש ל-role שישתמש בה (anon ו/או authenticated לפי הצורך) לצד ה-RLS policy — לבדוק את זה כחלק אינטגרלי מכתיבת ה-migration, לא לחכות ל"permission denied" בפרודקשן. אבל התיקון כשמתגלה תקלה נשאר תמיד ממוקד לטבלה הספציפית, לא grant גורף על כל הסכמה — זו החלטת עיצוב, לא רק אילוץ טכני.**
- **[2026-07-11] order-status page חסר RLS/GRANT לגמרי (טרם תוקן)** — `app/[locale]/(public)/order-status/page.tsx` מבצע `.from("orders").select(...).eq(order_number).eq(customer_email).single()` כ-anon, אבל אין שום anon SELECT policy על orders (רק "orders: admin all" ל-authenticated). כרגע הפיצ'ר הזה כנראה שבור. **אזהרה: אסור לתקן עם `USING (true)` — זה יחשוף את כל טבלת ההזמנות (שם/טלפון/אימייל/כתובת) לכל מי שיקרא ל-REST API בלי הפילטרים.** **תיקון: אין service role key בפרויקט הזה (`lib/supabase/server.ts` משתמש ב-anon key גם בצד שרת) — הפתרון הוא function עם SECURITY DEFINER (כמו is_admin/create_order) שמאמתת order_number+email בצד ה-DB ומחזירה רק שדות בטוחים, לא USING(true) ולא service role.**
- **[2026-07-11] RLS מסנן גם את RETURNING, לא רק SELECT רגיל** — `.insert({...}).select(...).single()` מ-anon נגד טבלה בלי anon SELECT policy (כמו orders, שבכוונה אין לה כזו כי יש בה PII) מצליח ב-INSERT אבל מקבל 0 שורות מה-RETURNING כי RLS מסנן אותו לפי SELECT policies — `.single()` זורק שגיאה מבוקרת (PGRST116) בלי שום console error (זה לא exception שנזרק, זה `{error}` רגיל שמוחזר). **תוקן ב-`010_order_insert_rpc.sql`: הפתרון הוא function עם SECURITY DEFINER (`create_order`) שעושה את ה-INSERT ומחזירה רק scalar (order_number), לא row מהטבלה — עוקפת RLS לגמרי בלי לחשוף SELECT policy כלשהי. אותו pattern בדיוק צריך לשמש לתיקון order-status. מסקנה כללית: כל INSERT/UPDATE מ-anon עם `.select()`/`RETURNING` על טבלה שיש בה PII (שאין לה anon SELECT policy בכוונה) חייב לעבור דרך SECURITY DEFINER function, לא insert ישיר.**
- **[2026-07-11] Storage buckets גם צריכים RLS policy — לא רק טבלאות** — כל bucket חדש ב-Supabase Storage שנגיש ל-`anon` (למשל "logos" בתהליך ההזמנה הציבורי) חייב policies מפורשים על `storage.objects` (INSERT/SELECT ל-anon), אחרת מתקבל 400 מה-Storage API. buckets שמשמשים רק את הפאנל המנהלי (products/catalog/gallery) עובדים כי הם נגישים רק ב-authenticated context — לא הוכיח שה-RLS מוגדר נכון ל-anon. תמיד לבדוק/להוסיף migration עם `storage.objects` policies כשמוסיפים bucket חדש שנגיש ללקוחות לא-מחוברים.
- **[2026-07-11] SECURITY DEFINER functions חייבות `SET search_path = public` מפורש** — `create_order()`/`validate_coupon()`/`redeem_coupon()` הפנו לטבלאות (`orders`/`coupons`) בלי schema qualification, וקרסו עם `relation "orders" does not exist` **בזמן ריצה בפועל דרך `.rpc()`**, למרות שהפונקציה עצמה נמצאה והורצה בהצלחה. הסיבה: SECURITY DEFINER רץ עם ההרשאות של הבעלים אבל לא מקבל search_path קבוע אוטומטית — הוא יורש את ה-search_path של הסשן הקורא בזמן הריצה, ו-`.rpc()` (בניגוד ל-`.from()` שמנוהל ע"י PostgREST) לא בהכרח פותר `public` כברירת מחדל. תוקן ב-`012_security_definer_search_path.sql` — כל SECURITY DEFINER function חדשה **מחייבת** `SET search_path = public` + schema-qualify (`public.table_name`) על כל reference פנימי, מההתחלה, לא רק אחרי שנתקלים בבאג. `is_admin()` הישנה לא נגעה בה (לא דווחה כשבורה) אבל ראוי לבדוק אותה גם באותו pattern בעתיד אם עולה בעיה דומה.
  - **[המשך] אותה שגיאה בדיוק נשארה אחרי 012 — הסיבה: triggers שמופעלים מתוך הפונקציה גם צריכים תיקון, לא רק הפונקציה עצמה.** `generate_order_number()` (trigger BEFORE INSERT על orders, ממיגרציה 001 המקורית) הפנה ל-`orders` בלי schema qualification, ולא נגעתי בו כשתיקנתי את שלוש הפונקציות ב-012 — הוא ממשיך לרוץ עם ה-INSERT בכל פעם ש-create_order() מכניס שורה. **לקח קריטי: כשמאבחנים "relation X does not exist" בתוך פעולה מורכבת (insert + trigger), לא מספיק לתקן את הפונקציה הראשית — צריך למפות את כל השרשרת (triggers, פונקציות מקוננות) ולוודא שלכולן יש schema qualification/search_path תקין.** אבחון: ביקשתי מהמשתמש להריץ `select proname, prosrc, proconfig from pg_proc where proname in (...)` כדי לוודא בפועל מה יש ב-DB במקום להמשיך לנחש — גילה שהשלוש שתוקנו אכן תוקנו נכון, וצמצם את החשד לפונקציה הרביעית שלא נגעתי בה. תוקן ב-`013_order_number_trigger_search_path.sql`.

## Key Learnings (עדכון 2026-05-28)

- **מבנה app/**: `app/[locale]/(public)/` לדפים ציבוריים, `app/[locale]/layout.tsx` מחזיק CartProvider+CartDrawer+CartRecoveryPopup.
- **ExpandingCards**: משמש בדף הבית ישירות מתחת להירו (לא רק ב-/catalog). בחירת סגנון = גריד פשוט.
- **ProductCard**: aspect-ratio 4/5, rounded-xl, QuickAddOverlay בdesktop hover, QuickAddMobileButton בmobile.
- **CartContext**: localStorage key `rina-kadosh-cart`, פג תוקף 7 ימים, ולידציית min_type/min_value.
- **Supabase MCP**: HTTP transport, צריך אימות מחדש בכל reload של IDE. auth זמני לסשן בלבד.
- **WhatsApp**: `972533721938` (NEXT_PUBLIC_WHATSAPP_NUMBER).
- **design-references/**: מוחרג מ-tsconfig כדי שלא ייכלל ב-build.

## Decision Log

- **[2026-05-27] הסרת product_variants** — המשתמש הבהיר שכל מוצר קיים בסוג אחד. הטבלה נוספה בהצעה טכנית ונדחתה.
- **[2026-05-27] חבילות כסקשן לא דף** — שמירה על זרימת ניווט רציפה: event → style → מוצרים + חבילות יחד.
- **[2026-05-27] Cart: גם drawer וגם עמוד /cart** — המשתמש רוצה את שניהם. Drawer לחוויה מהירה, /cart לטופס מלא.
- **[2026-05-27] Hero — המתנה לתמונות** — לא לנעול עיצוב Hero לפני שיש תמונות מוצר. PrismaHero נדון אבל לא אומץ.
- **[2026-05-27] next-intl לדו-לשוניות** — עברית RTL ↔ אנגלית LTR, toggle ב-Navbar.
- **[2026-07-24] ExpandingEventCards נשמר כמו שהוא** — המשתמש בחר לשמר את אינטראקציית ה-hover-expand הקיימת (במקום הכרטיסים הסטטיים שבהדמיה) ורק לעדכן עיצוב. סיבה: זה כבר ה"wow factor" שהוחלט עליו קודם (ראה [[design_references]]).
- **[2026-07-24] ProcessSteps מחליף את WhySection** — הבאנר "3 השלבים" החדש מההדמיה מחליף לגמרי את סקשן 3 היתרונות (מברשת/יהלום/לב). `WhySection.tsx` נמחק בפועל (לא רק import).
- **[2026-07-24] תמונת ה-Hero בהדמיה = placeholder, לא סופית** — ה-Hero החדש נבנה בלי תמונת מוצר (רק רקע שיש + טיפוגרפיה + דקורציה) כי ההחלטה הקודמת (Hero ממתין לצילומי מוצר אמיתיים) עדיין בתוקף. אל תוסיף קומפוזיציית תמונה ל-Hero עד שיגיעו צילומים אמיתיים.
- **[2026-07-24] סבב תיקונים ראשון אחרי ה-reskin — 3 בעיות שהמשתמש ציין:** (1) פונטים — תוקן, ראה [[Do-Not-Repeat]] על רישיונות. (2) קומפוזיציית כרטיסי סגנונות עיצוב ב-`/catalog/[event]` — תוקנה לגמרי: המבנה האמיתי בהדמיה הוא **רשימה אנכית של שורות מלאות-רוחב עם יישור מתחלף (זיגזג ימין/שמאל)**, לא grid של 3 עמודות שוות כמו שבניתי בהתחלה. כל שורה = ריבוע זית+פאטרן (עם עיגול תמונה חופף מלמעלה-חוץ כשיש תמונה, וכוכבית טרהקוטה) לצד בלוק טקסט (שם באנגלית בפונט script + שם בעברית + כפתור CTA). (3) חלק עליון של דף המוצרים — עדיין ממתין לצילומי מסך חדשים מהמשתמש, טרם טופל.
- **[2026-07-24] מקור אמת עיצובי חדש: הדמיית Illustrator אישית** — המשתמש עיצב הדמיה מלאה של המראה הרצוי באילוסטרייטור, במקום להסתמך רק על איסוף השראות מספריית קומפוננטות. חלק מהעיצוב יומר לתמונות רקע (טקסטורות אמיתיות בלבד), חלק נבנה מחדש בקוד (צבעים/גרדיאנטים/דפוסים). ראה [[design_references]] בזיכרון הפרויקט לפירוט מלא. תיקיית המקור: `ATAR/` (לא ב-git — עבד ישירות מהתיקייה).
- **[2026-07-24] שלב 1 של ה-reskin בוצע במלואו** — tokens (טרהקוטה/זית) + נכסים (`public/images/logo.png`, `public/textures/marble-bg.jpg`) + Navbar/Footer/BottomNav + HeroSection (רקע שיש, בלי תמונת placeholder — ה-Hero עדיין ממתין לצילומי מוצר אמיתיים) + ExpandingEventCards (רק reskin, האינטראקציה hover-expand נשמרה כמו שהיא) + ProcessSteps חדש (מחליף את WhySection שנמחק) + `/catalog/[event]` (כרטיסי קולקציה: עיגול תמונה + ריבוע זית עם GeometricPattern + כפתור CTA טרהקוטה) + `/catalog/[event]/[style]` (hero/bundles reskin, ProductCard תוקן ל-`aspect-[4/5]`). תוכנית מלאה שמורה ב-`.claude/plans/crispy-dreaming-mccarthy.md`. **נשאר לשלב הבא:** קבצי פונט (script + עברי) — עדיין fallback מערכת.
- **[2026-07-24] GeometricPattern — קומפוננטה חדשה ב-`components/ui/GeometricPattern.tsx`** — משחזרת בקוד (SVG `<pattern>`, לא PNG) את דפוס חצאי-העיגולים/עיגולים מ-`ATAR/Asset 3@2x.png`. זו לא טרייסינג פיקסל-מדויק של הקובץ המקורי — גריד 4×4 עם 5 מוטיבים (circle/up/down/left/right) שנבחרו ידנית לחקות את אותה "שפה" ויזואלית. Props: `color` (ברירת מחדל currentColor), `opacity`, `size`, `strokeWidth`. שימוש בכל מקום שצריך את הדפוס בתור דקורציה (לא תמונה) — כרגע ב-ExpandingEventCards fallback, HeroSection accents, כרטיסי קולקציה ב-/catalog/[event].
- **[2026-07-24] פונטים מותאמים — `app/fonts/index.ts`** — `next/font/local`, שני משתני CSS: `--font-mekomi` (עברית, 5 משקלים, מוזרם ל-`[dir="rtl"] body` ב-globals.css) ו-`--font-script` (אנגלי script, `happyBirthday.ttf`, זמין דרך Tailwind `font-script`). מוגדר ב-`tailwind.config.ts` תחת `theme.extend.fontFamily`. יש להטמיע את שני ה-variable classes ב-`<body>` ב-`app/[locale]/layout.tsx`.
- **[2026-07-24] openwolf designqc — quirks שגילינו:** (א) ב-Bash tool (git-bash), נתיבים שמתחילים ב-`/` בפרמטר `--routes` עוברים MSYS path conversion (`/he` הופך ל-`C:/Program Files/Git/he`) — **חובה** להריץ עם `MSYS_NO_PATHCONV=1` לפני הפקודה. (ב) `--routes` מקבל **מספר ארגומנטים מופרדים ברווח**, לא רשימה מופרדת בפסיקים — פסיק גורם לזה להתייחס לזה כ-route אחד ארוך ושגוי. (ג) כל הרצה חדשה **מוחקת** את `.wolf/designqc-captures/` הקודם (לא מצרפת) — יש לקרוא (Read) את הצילומים מיד אחרי כל capture לפני שמריצים route נוסף, אחרת הם אובדים.
- **[2026-07-27] סבב תיקונים שני אחרי ה-reskin — לפי ניתוח מחודש של הדמיות ATAR:** (1) ניווט: תוויות שונו ל-Welcome / האירועים שלכם / לקוחות מרוצים / נשמח להכיר (תואם מדויק להדמיה `ATAR/‏‏לכידה.JPG1.JPG`), עם ✦ מפריד בין הפריטים ורקע זית קבוע (לא עוד שקוף-עד-גלילה). (2) `InspirationSection.tsx` ("כי כל פרט קטן — מספר סיפור גדול") נמחק לגמרי לפי בקשה מפורשת. (3) `ProcessSteps` — הוחלף רק הצבע (כתום→זית) כפתרון ביניים; **העיצוב המלא ממתין לתמונת הדמיה שהמשתמש הודיע שישלח, אל תיגע שוב בלי אישור/תמונה חדשה.** (4) products.display_order + גרירה בקטלוג + og_image לסגנונות — ראה [[Key Learnings]]. (5) רקע שיש (bg-marble) נוסף מאחורי גריד המוצרים ב-`/catalog/[event]/[style]`, תואם למה שכבר קיים ב-`/catalog/[event]`.
- **[2026-07-27] מיגרציה 014 נכתבה אך לא הורצה — Supabase MCP לא היה מאומת בסשן זה** — הקובץ `supabase/migrations/014_products_display_order.sql` מוכן אבל צריך הרצה (MCP tools לא נטענו, המערכת דיווחה "requires authentication"). **בתחילת הסשן הבא: לבדוק אם ה-MCP מחובר לפני שמניחים שאפשר להריץ מיגרציות אוטומטית; אם לא — לבקש מהמשתמש לאמת מחדש (`/mcp`) או להריץ את ה-SQL ידנית ב-Supabase Studio.**
- **[2026-07-23] מקור אמת עיצובי חדש: הדמיית Illustrator אישית** — המשתמש עיצב הדמיה מלאה של המראה הרצוי באילוסטרייטור, במקום להסתמך רק על איסוף השראות מספריית קומפוננטות. חלק מהעיצוב יומר לתמונות רקע, חלק ייבנה מחדש בקוד. ראה [[design_references]] לפירוט. ממתינים לקבצי export מהמשתמש לפני המשך.
